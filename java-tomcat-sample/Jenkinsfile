pipeline{
    agent any
    stages{
        stage('Build Application'){
            steps{
                bat 'mvn -f jenkins-tomcat-repo/pom.xml clean package'
            }
            post{
                success{
                    echo "Now archiving the artifacts..."
                    archiveArtifacts artifacts: '**/*.war'
                }
            }
        }

        stage('Deploy to Staging env'){
            steps{
                build job: 'sat-deploy-staging'
            }
        }
    }
}